---
summary: "Environment and Tool specific notes"
---
# Tools and Environment

This space is reserved for specific configurations for skills (like specific folder paths, API keys for external services, etc.).
Ensure you have the required model provider keys (e.g., `OPENAI_API_KEY`) set in your environment.
